{
  "name": "chatbot-backend",
  "version": "1.0.0",
  "description": "Chatbot API with backend and frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "setup-production": "cd client && npm install --production=false && npm run build && cd .. && npm install",
    "build-client": "cd client && npm run build",
    "install-client": "cd client && npm install --production=false",
    "deploy": "npm run build-client && npm install && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-async-errors": "^3.1.1",
    "mongoose": "^8.6.3"
  }
}
